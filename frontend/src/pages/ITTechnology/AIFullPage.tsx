import React from "react";
import AIPage from "./AIPage";

const AIFullPage: React.FC = () => (
  <div className="h-screen flex flex-col overflow-hidden">
    <AIPage />
  </div>
);

export default AIFullPage;

import React from "react";
import MLPage from "./MLPage";

const MLFullPage: React.FC = () => (
  <div className="h-screen flex flex-col overflow-hidden">
    <MLPage />
  </div>
);

export default MLFullPage;

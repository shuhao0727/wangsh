FROM shuhao07/wangsh-pythonlab-worker:1.5.14
COPY app/ /app/app/
USER appuser
EXPOSE 8000
CMD ["sh", "-c", "python /app/scripts/bootstrap_db.py && alembic -c /app/alembic.ini upgrade head && python -m uvicorn main:app --host 0.0.0.0 --port 8000 --workers ${UVICORN_WORKERS:-1}"]

# WangSh 项目改进检查清单（详细执行计划版）

> **最后更新**：2026-04-11
> 
> **状态**：当前活跃主计划文档
> 
> **说明**：本文档整合了原有的多个计划文档，作为统一的改进执行计划。

本清单用于把当前项目的改进方向落到可执行计划。每个任务都尽量补齐以下信息：
- 做什么
- 为什么先做
- 开始前需要什么
- 具体怎么做
- 做完要产出什么
- 用什么命令或方式验证
- 有哪些风险与回退点

---

## 一、计划使用规则

### 1. 状态规则
- [ ] 未开始
- [x] 已完成

### 2. 更新规则
- [ ] 每周至少更新一次本文件
- [ ] 每次更新同步填写“本周完成 / 下周计划 / 阻塞项”
- [ ] 每个阶段结束后补一份阶段复盘
- [ ] 若优先级变化，直接在本文件中调整，不另开平行版本

### 3. 记录模板

#### 本周完成
- [ ] 待更新

#### 下周计划
- [ ] 待更新

#### 阻塞项
- [ ] 暂无

#### 阶段复盘模板
- [ ] 本阶段目标是否完成
- [ ] 主要收益是什么
- [ ] 哪些任务延期
- [ ] 延期原因与下一步调整

---

## 二、当前基线（2026-04-10）

### 已确认事实
- [x] 后端已经具备较完整的 `pytest` 目录结构，重点目录包括 `backend/tests/group_discussion/`、`backend/tests/pythonlab/`、`backend/tests/assessment/`、`backend/tests/system/`
- [x] 前端当前仍未具备可运行的单元测试命令，`frontend/package.json` 中 `test` 仍是占位脚本
- [x] Prometheus 指标端点已经存在，位置为 `backend/app/api/endpoints/system/metrics.py`
- [x] PythonLab 已有前端专项 smoke 脚本入口，当前命令为 `cd frontend && npm run pythonlab:smoke`
- [x] 本轮优先拆分的四个后端文件都已超过 1200 行

### 当前高优先级大文件
- [x] `backend/app/services/agents/group_discussion.py`：1341 行
- [x] `backend/app/api/pythonlab/ws.py`：1314 行
- [x] `backend/app/api/pythonlab/flow.py`：1277 行
- [x] `backend/app/services/assessment/session_service.py`：1251 行

### 当前测试与脚本入口

#### 后端
- [x] 全量测试：`cd backend && pytest -q`
- [x] 小组讨论：`cd backend && pytest -q tests/group_discussion`
- [x] PythonLab：`cd backend && pytest -q tests/pythonlab`
- [x] 测评域：`cd backend && pytest -q tests/assessment`
- [x] 系统监控：`cd backend && pytest -q tests/system`

#### 前端
- [x] 类型检查：`cd frontend && npm run type-check`
- [x] 构建：`cd frontend && npm run build`
- [x] PythonLab UI 冒烟：`cd frontend && npm run pythonlab:smoke`
- [x] UI 审计：`cd frontend && npm run ui:audit`
- [x] UI 页面治理验证：`cd frontend && npm run ui:page:verify`

### 本轮总目标
- [ ] 优先处理高风险大文件，降低维护成本和回归风险
- [ ] 补齐后端关键域回归能力与前端单测基础设施
- [ ] 以证据驱动方式做数据库与性能优化
- [ ] 把监控、仪表板和告警链路补齐到可值班状态

---

## 三、总体实施顺序

### 建议排期（7 周）

#### 第 1 周
- [ ] 梳理 `group_discussion.py` 的导入边界与测试缺口
- [ ] 补齐 `group_discussion` 最小回归测试
- [ ] 开始拆分 `group_discussion.py`

#### 第 2 周
- [ ] 完成 `group_discussion.py` 拆分收尾
- [ ] 补齐 `ws.py` 最小回归测试
- [ ] 开始拆分 `ws.py`

#### 第 3 周
- [ ] 完成 `ws.py` 与 `flow.py` 关键拆分
- [ ] 启动 `session_service.py` 拆分
- [ ] 开始采集数据库慢查询样本

#### 第 4 周
- [ ] 搭建前端 `Vitest + React Testing Library`
- [ ] 补齐首批前端组件测试
- [ ] 开始关键流程 Playwright / smoke 规划

#### 第 5 周
- [ ] 做数据库索引复核与查询优化
- [ ] 做无障碍优化第一轮
- [ ] 做前端性能基线与首轮优化

#### 第 6 周
- [ ] 补齐 Prometheus 抓取接入
- [ ] 建第一版 Grafana 仪表板
- [ ] 梳理告警规则分级

#### 第 7 周
- [ ] 完成告警链路接入与演练
- [ ] 输出阶段复盘
- [ ] 把持续性工作切换到月度 / 季度节奏

### 排期原则
- [ ] 所有高风险文件拆分都遵循“先补最小测试，再拆分，再回归”
- [ ] 数据库优化先采样再动索引，不做拍脑袋优化
- [ ] 监控与告警只对真实已接入指标做承诺
- [ ] 前端测试基础设施与 E2E 分开推进，不在一周内同时起两套重工作流

---

## 四、第一阶段：基础优化（第 1-2 周）

### 任务 1：拆分高风险超大文件

**目标：**
- [ ] 降低四个超大文件的阅读、修改和回归成本
- [ ] 明确模块边界，减少横向耦合
- [ ] 为后续补测试、做性能优化和问题排查提供更清晰的结构

**建议工期：**
- [ ] 8-10 个工作日

**依赖：**
- [ ] 对现有导入关系有基础梳理
- [ ] 至少有最小回归测试或 smoke 验证入口
- [ ] 明确对外兼容接口，不随拆分改变调用方式

**统一执行原则：**
- [ ] 每个文件拆分前先列出“对外导出函数 / 内部函数 / 依赖模块 / 潜在共享状态”
- [ ] 优先迁移纯函数和工具函数
- [ ] 最后再迁移依赖数据库、缓存、配置或跨模块调用的函数
- [ ] 统一通过 `__init__.py` 做兼容导出
- [ ] 拆分期间不改 API 路径、不改响应结构、不改外部服务契约

#### 1.1 拆分 `group_discussion.py`

**目标：**
- [ ] 将工具函数、提示词函数、会话编排逻辑、分析逻辑拆开
- [ ] 控制跨系统逻辑与纯业务逻辑的耦合

**建议产出结构：**
- [ ] `backend/app/services/agents/group_discussion/__init__.py`
- [ ] `backend/app/services/agents/group_discussion/core.py`
- [ ] `backend/app/services/agents/group_discussion/prompts.py`
- [ ] `backend/app/services/agents/group_discussion/session_service.py`
- [ ] `backend/app/services/agents/group_discussion/analysis_service.py`
- [ ] `backend/app/services/agents/group_discussion/cross_system_service.py`

**详细步骤：**
- [ ] 盘点当前文件中的所有函数，标注“纯函数 / I/O 函数 / 跨模块函数”
- [ ] 梳理现有导入方，明确哪些符号不能改名
- [ ] 在 `backend/tests/group_discussion/` 中补齐最小回归测试
- [ ] 先迁移 `_gd_key`、`_normalize_*`、`_display_name` 等纯工具函数到 `core.py`
- [ ] 再迁移 `_default_prompt`、`_default_compare_prompt`、`_student_profile_prompt`、`_cross_system_prompt` 到 `prompts.py`
- [ ] 把会话创建、查找、归并等主业务逻辑迁移到 `session_service.py`
- [ ] 把分析类逻辑拆到 `analysis_service.py`
- [ ] 把跨系统数据拼接或横向调用拆到 `cross_system_service.py`
- [ ] 在 `__init__.py` 中保留兼容导出
- [ ] 全量搜索旧导入并修正

**交付物：**
- [ ] 完成新的包结构
- [ ] 有最小回归测试覆盖核心路径
- [ ] 保留对外兼容导出说明

**验收：**
- [ ] `cd backend && pytest -q tests/group_discussion`
- [ ] 相关 AI 智能体 / 小组讨论主流程手动验证通过
- [ ] 无新增循环依赖
- [ ] 导入路径兼容旧调用方

**风险与回退：**
- [ ] 风险：隐藏的模块级共享状态在拆分后失效
- [ ] 风险：提示词函数迁移后引入导入环
- [ ] 回退方案：以单文件提交为单位拆分，任何一步出问题可只回退当前拆分提交

#### 1.2 拆分 `ws.py`

**目标：**
- [ ] 将 WebSocket 连接管理、协议校验、消息处理、桥接类职责分离
- [ ] 减少调试控制逻辑修改时的回归面

**建议产出结构：**
- [ ] `backend/app/api/pythonlab/ws/__init__.py`
- [ ] `backend/app/api/pythonlab/ws/bridge.py`
- [ ] `backend/app/api/pythonlab/ws/connection.py`
- [ ] `backend/app/api/pythonlab/ws/validation.py`
- [ ] `backend/app/api/pythonlab/ws/handlers.py`
- [ ] `backend/app/api/pythonlab/ws/utils.py`

**详细步骤：**
- [ ] 梳理 `DapSessionBridge` 的职责与依赖边界
- [ ] 列出当前 WebSocket 消息类型、握手流程、异常分支
- [ ] 在 `backend/tests/pythonlab/` 中补足最小回归测试
- [ ] 将 `DapSessionBridge` 单独迁移到 `bridge.py`
- [ ] 将连接建立、断开、重连、接管逻辑迁移到 `connection.py`
- [ ] 将参数校验、消息格式校验、权限校验迁移到 `validation.py`
- [ ] 将消息分发与事件处理迁移到 `handlers.py`
- [ ] 将通用工具函数迁移到 `utils.py`
- [ ] 在 `__init__.py` 中封装对外兼容接口
- [ ] 对照前端 smoke 场景逐项回归

**交付物：**
- [ ] 更清晰的 WebSocket 包结构
- [ ] 文档化的消息流与异常流说明
- [ ] 至少一套可重复执行的专项 smoke

**验收：**
- [ ] `cd backend && pytest -q tests/pythonlab`
- [ ] `cd frontend && npm run pythonlab:smoke`
- [ ] 必要时补跑真实 Chrome 与 WebKit 专项验证
- [ ] 连接、断线恢复、调试继续、单步执行等主路径保持正常

**风险与回退：**
- [ ] 风险：重连 / 接管逻辑往往依赖隐式状态，拆分时最容易出错
- [ ] 风险：前端调试控制区对 hover / focus / pointer 改动很敏感
- [ ] 回退方案：先保留旧入口和包装层，确认 smoke 稳定后再清理兼容代码

#### 1.3 拆分 `flow.py`

**目标：**
- [ ] 把构建上下文、节点处理、流程拼装、校验、导出逻辑分层
- [ ] 降低后续优化节点转换、导出和错误提示时的耦合成本

**建议产出结构：**
- [ ] `backend/app/api/pythonlab/flow/__init__.py`
- [ ] `backend/app/api/pythonlab/flow/context.py`
- [ ] `backend/app/api/pythonlab/flow/node_utils.py`
- [ ] `backend/app/api/pythonlab/flow/flow_utils.py`
- [ ] `backend/app/api/pythonlab/flow/validation.py`
- [ ] `backend/app/api/pythonlab/flow/export.py`

**详细步骤：**
- [ ] 梳理 `_BuildCtx` 的使用点与生命周期
- [ ] 识别节点规范化、边关系处理、导出逻辑的边界
- [ ] 补齐 `flow` 归一化和异常路径回归测试
- [ ] 先迁移 `_BuildCtx` 到 `context.py`
- [ ] 再迁移节点转换与节点查询函数到 `node_utils.py`
- [ ] 将流程级拼装逻辑迁移到 `flow_utils.py`
- [ ] 将校验逻辑迁移到 `validation.py`
- [ ] 将导出和序列化相关逻辑迁移到 `export.py`
- [ ] 保持原始入口函数不变，必要时用薄包装兼容旧导入

**交付物：**
- [ ] 流程构建相关代码按职责拆开
- [ ] 节点与导出逻辑可单独定位和测试

**验收：**
- [ ] `cd backend && pytest -q tests/ai_agents/test_flow_optimize_normalization.py tests/pythonlab`
- [ ] 节点构建、导出、错误提示与拆分前行为一致

**风险与回退：**
- [ ] 风险：部分函数可能既做校验又做转换，拆分时容易漏掉副作用
- [ ] 回退方案：先做薄拆分，不在第一次拆分中同步重构逻辑

#### 1.4 拆分 `session_service.py`

**目标：**
- [ ] 将解析、评分、分析和工具函数分层
- [ ] 降低测评会话逻辑继续膨胀的风险

**建议产出结构：**
- [ ] `backend/app/services/assessment/session/__init__.py`
- [ ] `backend/app/services/assessment/session/parser.py`
- [ ] `backend/app/services/assessment/session/grading.py`
- [ ] `backend/app/services/assessment/session/analysis.py`
- [ ] `backend/app/services/assessment/session/utils.py`

**详细步骤：**
- [ ] 盘点当前文件中每个函数的输入输出与副作用
- [ ] 确认和 `assessment` 相关 schema、model、service 的引用边界
- [ ] 在 `backend/tests/assessment/` 中补齐最小回归测试
- [ ] 先迁移纯解析逻辑到 `parser.py`
- [ ] 再迁移评分规则和结果计算逻辑到 `grading.py`
- [ ] 将画像、结果分析或统计逻辑迁移到 `analysis.py`
- [ ] 将通用工具函数迁移到 `utils.py`
- [ ] 用 `__init__.py` 保持旧入口兼容

**交付物：**
- [ ] 测评会话逻辑按职责拆分
- [ ] 关键服务调用边界更清晰

**验收：**
- [ ] `cd backend && pytest -q tests/assessment`
- [ ] 测评会话创建、答题、评分、画像链路通过

**风险与回退：**
- [ ] 风险：评分规则常常夹杂数据清洗和统计逻辑，拆分时要先保守迁移
- [ ] 回退方案：优先按文件归类，不在第一轮引入新抽象

### 任务 2：优化数据库性能

**目标：**
- [ ] 以慢查询和执行计划为依据，找出真正值得优化的 SQL 和索引
- [ ] 避免重复建设已存在索引

**建议工期：**
- [ ] 3-4 个工作日

**依赖：**
- [ ] 能访问数据库监控或样本
- [ ] 能执行 `EXPLAIN ANALYZE` 或等价分析
- [ ] 明确关键业务请求路径

#### 2.1 证据采集

**详细步骤：**
- [ ] 启用 PostgreSQL 慢查询日志或等价采样机制
- [ ] 打开 `pg_stat_statements` 或同类查询统计能力
- [ ] 采集高峰时段样本
- [ ] 按耗时、调用次数、总时间排序生成慢查询列表
- [ ] 为 Top N 查询补业务来源说明

**交付物：**
- [ ] 一份慢查询清单
- [ ] 一份高频查询清单
- [ ] 关键 SQL 对应接口 / 功能说明

#### 2.2 现有索引复核

**详细步骤：**
- [ ] 盘点 `znt_group_discussion_sessions` 现有索引
- [ ] 盘点 `znt_group_discussion_messages` 现有索引
- [ ] 盘点 `znt_conversations` 现有索引
- [ ] 标记“已存在但命中不足”和“完全缺失”的索引场景
- [ ] 对候选新增索引执行 `EXPLAIN ANALYZE` 前后对比
- [ ] 将确定新增的索引通过 Alembic 迁移管理

**交付物：**
- [ ] 索引复核表
- [ ] 新增索引候选清单
- [ ] 对应迁移脚本

#### 2.3 查询与缓存优化

**详细步骤：**
- [ ] 优先审查 `group_discussion` 相关查询
- [ ] 优先审查 `assessment` 相关查询
- [ ] 查找 N+1 查询与重复查询
- [ ] 明确哪些查询适合在 SQL 层优化，哪些适合做缓存
- [ ] 对拟引入缓存的场景补失效策略说明

**验收：**
- [ ] 输出查询性能报告
- [ ] 每个新增索引都有样本 SQL 支撑
- [ ] 关键查询 P95 比基线改善
- [ ] 无明显写入回归与锁冲突

**风险与回退：**
- [ ] 风险：误加复合索引导致写入性能变差
- [ ] 风险：缓存策略不清晰会带来脏数据
- [ ] 回退方案：所有变更都走迁移和配置开关，避免手工不可逆调整

### 任务 3：加强无障碍访问

**目标：**
- [ ] 在不破坏现有 UI 结构的前提下补齐高频页面的可访问性
- [ ] 优先改善真实用户影响最大的按钮、表单、焦点与动态提示

**建议工期：**
- [ ] 3-4 个工作日

**依赖：**
- [ ] 有待检查页面清单
- [ ] 有至少一种人工辅助验证方式

#### 3.1 范围确定
- [ ] 第一轮优先检查 `frontend/src/pages/Home/index.tsx`
- [ ] 第一轮优先检查 `frontend/src/components/ui/button.tsx`
- [ ] 第一轮优先检查 `frontend/src/components/ui/input.tsx`
- [ ] 第二轮扩展到高频管理后台页面

#### 3.2 详细步骤
- [ ] 扫描所有图标按钮，补缺失的可访问名称
- [ ] 对纯装饰图标统一加 `aria-hidden="true"`
- [ ] 检查表单是否存在 `label`、错误提示和 `aria-describedby`
- [ ] 检查是否需要 `aria-invalid`
- [ ] 检查模态框、抽屉、弹层是否有 `aria-labelledby` / `aria-describedby`
- [ ] 补齐“跳过导航”链接和可见焦点样式
- [ ] 对动态内容更新区域评估是否需要 `aria-live`

#### 3.3 验证步骤
- [ ] 手工执行 Tab / Shift+Tab 焦点巡检
- [ ] 运行 WAVE 扫描
- [ ] 运行 Lighthouse 无障碍检查
- [ ] 至少完成一次 NVDA 或 VoiceOver 人工验证

**交付物：**
- [ ] 高优先级页面无障碍整改清单
- [ ] 焦点样式与跳过导航入口
- [ ] 一份简短的人工验证记录

**验收：**
- [ ] Lighthouse 无障碍评分 >= 90
- [ ] WAVE 无严重错误
- [ ] 键盘可完整操作核心页面

**风险与回退：**
- [ ] 风险：滥用 `tabindex` 会打乱原生焦点顺序
- [ ] 风险：只看扫描工具而不做人工验证，容易漏掉真实交互问题

---

## 五、第二阶段：测试完善（第 3-5 周）

### 任务 4：补齐后端集成测试与前端组件测试

**目标：**
- [ ] 后端关键域具备可重复回归能力
- [ ] 前端建立可持续维护的单元测试基础设施

**建议工期：**
- [ ] 5-7 个工作日

#### 4.1 后端关键域测试补齐

**范围：**
- [ ] 认证 API
- [ ] PythonLab
- [ ] AI 智能体
- [ ] 测评域

**详细步骤：**
- [ ] 盘点现有测试覆盖空白
- [ ] 为每个域确定“主路径 + 1 个异常路径”最小回归集
- [ ] 为外部依赖使用 monkeypatch / fake service 控制输入输出
- [ ] 把一次性排障脚本和正式测试用例分离
- [ ] 更新 `backend/tests/README.md` 中新增模块说明

**交付物：**
- [ ] 一组可稳定执行的关键域回归测试
- [ ] 更新后的后端测试目录说明

**验收：**
- [ ] `cd backend && pytest -q tests/auth tests/pythonlab tests/assessment tests/group_discussion`
- [ ] 失败时能快速定位到具体模块

#### 4.2 前端单测基础设施

**范围：**
- [ ] 使用 `Vitest + React Testing Library`
- [ ] 不再新建基于 `Jest` 的新测试方案

**详细步骤：**
- [ ] 为 `frontend` 引入 `vitest`、`jsdom`、Testing Library 等依赖
- [ ] 建立 `vitest.config` 或等价配置
- [ ] 把 `npm test` 改成可运行命令
- [ ] 建立测试 setup 文件
- [ ] 先为 `Home`、`Button`、`Input` 补首批回归用例
- [ ] 约定哪些组件适合单测，哪些交给 Playwright

**交付物：**
- [ ] 可运行的前端测试命令
- [ ] 前端测试基础配置文件
- [ ] 至少 3 个高价值首批测试文件

**验收：**
- [ ] `cd frontend && npm test`
- [ ] `cd frontend && npm run type-check`
- [ ] 测试命令可在本地与 CI 使用

**风险与回退：**
- [ ] 风险：一次性为所有组件补测会造成节奏过重
- [ ] 建议：只先覆盖通用组件和首页关键行为

### 任务 5：实施端到端测试

**目标：**
- [ ] 为最关键用户路径建立浏览器级回归保障
- [ ] 将“发布前 smoke”和“完整 E2E”拆开治理

**建议工期：**
- [ ] 4-5 个工作日

#### 5.1 流程清单
- [ ] 用户注册登录流程
- [ ] PythonLab 调试流程
- [ ] AI 对话流程
- [ ] 课堂互动主流程

#### 5.2 环境准备
- [ ] 准备独立测试数据库
- [ ] 为 AI 或外部接口提供 mock / 测试配置
- [ ] 准备测试账号
- [ ] 准备基础测试数据工厂
- [ ] 明确 smoke 与 full E2E 的数据隔离策略

#### 5.3 详细步骤
- [ ] 先基于现有前端 smoke 入口梳理可复用场景
- [ ] 把“发布前必须跑”的流程定义为 smoke
- [ ] 把“夜间或主分支跑”的流程定义为 full E2E
- [ ] 为每条主流程补失败截图、日志和重试策略
- [ ] 为每条用例补“期望成功条件”定义

**交付物：**
- [ ] 一套最小 smoke 流程
- [ ] 一套完整 E2E 规划清单
- [ ] 环境与数据准备说明

**验收：**
- [ ] smoke 套件可在发布前稳定执行
- [ ] full E2E 可单独运行
- [ ] 报告中能直接定位失败步骤

**风险与回退：**
- [ ] 风险：把不稳定依赖直接接入 E2E 会造成持续误报
- [ ] 建议：第一轮先确保 smoke 稳，再扩大 full E2E

### 任务 6：优化前端性能

**目标：**
- [ ] 提高首页与高频页面的首屏体验
- [ ] 把性能优化建立在基线和具体资源体积之上

**建议工期：**
- [ ] 3-4 个工作日

#### 6.1 基线采集
- [ ] 对首页和高频页面做 Lighthouse 基线
- [ ] 梳理首屏关键资源、体积大依赖、渲染热点
- [ ] 盘点项目中真实使用 `<img>` 的页面
- [ ] 梳理哪些页面适合路由级分割

#### 6.2 优化执行
- [ ] 实施路由级代码分割
- [ ] 对重量级组件使用懒加载
- [ ] 优化字体加载与关键资源预加载
- [ ] 优化静态资源缓存策略
- [ ] 必要时引入统一图片懒加载组件
- [ ] 保持首屏骨架和可用性，不因懒加载造成空白或闪烁

#### 6.3 交付与验收
- [ ] 输出前后对比基线
- [ ] 记录每项优化的收益和副作用
- [ ] 如果“服务端渲染”要继续评估，单独列为后续调研，不纳入本阶段硬验收

**验收：**
- [ ] `cd frontend && npm run build`
- [ ] `cd frontend && npm run ui:audit`
- [ ] 首页与关键页面 Lighthouse 得分较基线改善

**风险与回退：**
- [ ] 风险：只做打包层优化而不验证真实页面体感
- [ ] 风险：图片懒加载滥用会影响首屏关键视觉

---

## 六、第三阶段：监控与发布保障（第 6-7 周）

### 任务 7：补齐 Prometheus 落地接入

**目标：**
- [ ] 让现有指标端点真正接入 Prometheus 抓取链路
- [ ] 让应用、数据库、缓存指标能统一汇总

**建议工期：**
- [ ] 2-3 个工作日

#### 7.1 应用指标
- [ ] 复核 `/system/metrics` 现有指标清单
- [ ] 梳理请求量、延迟、错误率、业务指标口径
- [ ] 检查指标命名、标签与采样周期是否清晰
- [ ] 如有缺失，补充指标说明文档

#### 7.2 基础设施指标
- [ ] 判断 Node Exporter 是否已接入
- [ ] 判断 PostgreSQL Exporter 是否已接入
- [ ] 判断 Redis Exporter 是否已接入
- [ ] 若未接入，补接入方案与部署步骤

#### 7.3 详细步骤
- [ ] 为发布环境配置 Prometheus 抓取目标
- [ ] 验证抓取状态和标签
- [ ] 对齐后端、数据库、缓存的采样周期
- [ ] 输出一份抓取链路说明

**交付物：**
- [ ] 抓取配置
- [ ] 指标口径说明
- [ ] Prometheus 目标健康状态确认记录

**验收：**
- [ ] Prometheus 能稳定抓取关键服务
- [ ] 至少一份 Grafana 仪表板可直接消费这些指标
- [ ] 系统指标与应用指标能交叉排障

### 任务 8：配置 Grafana 仪表板

**目标：**
- [ ] 建立值班可读、排障可用的仪表板

**建议工期：**
- [ ] 2 个工作日

#### 8.1 首批仪表板范围
- [ ] 应用健康总览
- [ ] API 性能与错误率
- [ ] 数据库与缓存状态
- [ ] 关键业务行为

#### 8.2 详细步骤
- [ ] 配置 Prometheus 数据源
- [ ] 配置 PostgreSQL 数据源
- [ ] 配置 Redis 数据源
- [ ] 为每个面板写清指标含义
- [ ] 区分“巡检面板”和“深挖面板”
- [ ] 控制面板数量，先做高价值版本

**交付物：**
- [ ] 一套首批 Grafana 仪表板
- [ ] 面板字段说明

**验收：**
- [ ] 面板刷新正常
- [ ] 关键字段无大面积空值
- [ ] 值班人员可用它定位常见问题

### 任务 9：设置关键指标告警

**目标：**
- [ ] 对服务不可用、性能退化、错误率异常和资源风险建立分级告警

**建议工期：**
- [ ] 2-3 个工作日

#### 9.1 告警规则设计
- [ ] P0：服务不可用
- [ ] P1：性能严重下降
- [ ] P2：错误率升高
- [ ] P3：资源接近阈值

#### 9.2 告警链路
- [ ] 邮件
- [ ] Slack / 钉钉
- [ ] 短信（可选）
- [ ] 升级路径和值班责任

#### 9.3 详细步骤
- [ ] 为每条告警写触发条件和恢复条件
- [ ] 明确抑制规则，减少告警风暴
- [ ] 配置通知模板，保证信息可读
- [ ] 做至少一次人工演练
- [ ] 对误报与漏报做一次复盘

**交付物：**
- [ ] 告警规则清单
- [ ] 通知渠道配置
- [ ] 值班与升级说明

**验收：**
- [ ] 至少完成一次演练
- [ ] 告警触发准确
- [ ] 通知链路可达

**风险与回退：**
- [ ] 风险：指标口径不清会导致告警不可信
- [ ] 风险：没有抑制规则会导致告警泛滥

---

## 七、第四阶段：质量维护（持续）

### 任务 10：每月代码质量评估

**目标：**
- [ ] 把质量治理从一次性项目变成持续性节奏

**建议节奏：**
- [ ] 每月固定 1 次

**详细步骤：**
- [ ] 收集代码复杂度数据
- [ ] 收集测试覆盖率数据
- [ ] 汇总技术债务数量
- [ ] 汇总安全漏洞状态
- [ ] 输出月度问题清单
- [ ] 为下月给出前 3 个优先行动

**交付物：**
- [ ] 月度质量报告
- [ ] 下月优先级建议

### 任务 11：季度架构审查

**目标：**
- [ ] 定期回看架构方向是否还适配当前业务和团队能力

**建议节奏：**
- [ ] 每季度 1 次

**详细步骤：**
- [ ] 收集架构决策记录
- [ ] 收集性能数据
- [ ] 收集用户反馈
- [ ] 收集团队意见
- [ ] 评估技术栈适配度
- [ ] 输出瓶颈、风险和下一季重点

**交付物：**
- [ ] 季度架构审查记录
- [ ] 下季度架构改进建议

### 任务 12：持续技术债务管理

**目标：**
- [ ] 把技术债务纳入稳定治理节奏，而不是临时清理

**详细步骤：**
- [ ] 在 Linear / Jira 建立债务台账
- [ ] 为债务分级：小 / 中 / 大
- [ ] 每周清理小债务
- [ ] 每月处理中债务
- [ ] 每季度推进高价值大债务
- [ ] 评估是否接入 SonarQube 或等价质量门禁

**交付物：**
- [ ] 技术债务台账
- [ ] 清理节奏说明
- [ ] 债务处理复盘记录

---

## 八、统一验收标准

### 代码质量
- [ ] 本轮新拆出的模块尽量控制在 500 行以内
- [ ] 历史大文件按优先级持续消化，不要求一次性全部达标
- [ ] 修改过的核心模块复杂度下降
- [ ] 不引入新的严重安全漏洞

### 测试质量
- [ ] 后端关键域具备稳定回归测试
- [ ] 前端具备可运行的单测命令
- [ ] 关键用户路径具备 smoke 或 E2E 覆盖
- [ ] 失败时能快速定位责任模块

### 性能质量
- [ ] 关键查询和关键接口有前后基线
- [ ] API 和数据库 P95 相较基线改善
- [ ] 首页与高频页面体感提升
- [ ] 优化不引入明显功能回退

### 运维质量
- [ ] 关键服务纳入监控
- [ ] 仪表板可用于巡检
- [ ] 告警经过演练
- [ ] 关键变更有回滚方案

---

## 九、风险管理

### 技术风险
- [ ] 文件拆分前保留回退路径
- [ ] 数据库变更通过 Alembic 管理
- [ ] 监控接入有降级预案
- [ ] 测试环境与生产环境隔离

### 资源风险
- [ ] 每项阶段任务有明确负责人
- [ ] 排期中预留测试与修复时间
- [ ] 监控系统维护责任明确
- [ ] 团队对新工具链有最小培训安排

### 实施风险
- [ ] 高风险任务拆成小步提交
- [ ] 对外部依赖设置阻塞预案
- [ ] 每阶段结束做复盘
- [ ] 若阶段延期，明确缩范围还是顺延，不模糊处理

---

## 十、文档与流程更新清单

### 第一阶段需补齐
- [ ] 文件拆分方案文档
- [ ] 数据库优化报告
- [ ] 无障碍改进记录

### 第二阶段需补齐
- [ ] 后端测试清单
- [ ] 前端测试接入说明
- [ ] E2E 运行手册
- [ ] 性能基线报告

### 第三阶段需补齐
- [ ] 监控接入说明
- [ ] Grafana 仪表板说明
- [ ] 告警配置与值班手册
- [ ] 故障排查手册

### 第四阶段需补齐
- [ ] 质量评估模板
- [ ] 架构审查记录模板
- [ ] 技术债务管理规范
- [ ] 持续改进流程

---

**最后更新：** 2026-04-10  
**下次评审：** 2026-04-17

# Checklist

- [ ] CSS: Form has `height: 100%` or `flex: 1`.
- [ ] CSS: Card Body has `flex: 1` and `min-height: 0`.
- [ ] CSS: Form Item Control chain has `flex: 1`.
- [ ] CSS: TextArea wrapper (`ant-input-affix-wrapper`) has `height: 100%`.
- [ ] CSS: TextArea element has `height: 100%` or `flex: 1`.
- [ ] React: Removed inline `height` styles that might conflict.
- [ ] Docker: Rebuilt and deployed.
